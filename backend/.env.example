# Configuration pour le backend Node.js

# Port du serveur
PORT=3005

# Configuration AWS
AWS_REGION=eu-west-1

# Nom de la table DynamoDB (sera remplacé par la valeur Terraform en production)
DYNAMODB_TABLE_NAME=tp2-bis-react-app-todos-production

# Pour les tests locaux avec DynamoDB Local (optionnel)
# AWS_ENDPOINT_URL=http://localhost:8000
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
